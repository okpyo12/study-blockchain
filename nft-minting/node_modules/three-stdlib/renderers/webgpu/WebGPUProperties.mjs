class WebGPUProperties {
  constructor() {
    this.properties = /* @__PURE__ */ new WeakMap();
  }
  get(object) {
    let map = this.properties.get(object);
    if (map === void 0) {
      map = {};
      this.properties.set(object, map);
    }
    return map;
  }
  remove(object) {
    this.properties.delete(object);
  }
  dispose() {
    this.properties = /* @__PURE__ */ new WeakMap();
  }
}
export {
  WebGPUProperties as default
};
